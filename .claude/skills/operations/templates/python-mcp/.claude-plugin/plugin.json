{
  "name": "{{plugin_name}}",
  "description": "{{description}}",
  "version": "0.0.0",
  "author": { "name": "{{author_name}}" },
  "mcpServers": {
    "{{short_name}}": {
      "command": "${CLAUDE_PLUGIN_ROOT}/bin/{{short_name}}",
      "args": [],
      "env": {
        "{{SHORT_NAME_UPPER}}_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
